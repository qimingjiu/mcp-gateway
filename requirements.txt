# MCP Gateway Core
# ⚠️ mcp 必须锁 1.x：2.0.0 起移除了 mcp.server.fastmcp（FastMCP 独立成包），
# 不锁版本重建即 ModuleNotFoundError 启动崩溃（2026-08-07 实战翻车）
mcp==1.29.0
uvicorn
python-dotenv
starlette
pydantic
requests
websockets

# Database
supabase

# LLM SDK
openai

# Vector Memory (Mem0 + Pinecone dual-write)
mem0ai
pinecone

# Web Search
tavily-python
duckduckgo-search

# Google Integration (Gmail / Calendar)
google-auth
google-auth-httplib2
google-api-python-client

# Multimedia (AI Music / Cover, optional)
replicate
